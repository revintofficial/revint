# V-M Legacy Delete — Prep Audit (DO NOT EXECUTE YET)

> Generated by V-M Track Owner subagent in Wave 2 (read-only audit mode).
> Master Coordinator persisted this file. Per master plan §3 V-M risk
> note + Open Decision §10.6, the actual delete requires (a) v2
> telemetry showing zero `legacy_*` event hits in the most recent
> release AND (b) a 14-day deprecation window. **This audit prepares
> the diff but does NOT execute it.**

---

## Files identified for deletion

**Primary surface (master envelope)**

- `src/components/app/leads/LegacyLeadDetailClient.tsx` (~2750 LOC) — original **5-tab** client (`overview` / `website` / `workers` / `reviews` / `outreach`); client-side data loading, hero, NBA, dossier, website/review workers UI, outreach stepper, dialogs, etc.

**Path correction:** master plan §3 V-M lists this at `src/app/app/leads/[id]/LegacyLeadDetailClient.tsx`. The actual file lives at `src/components/app/leads/LegacyLeadDetailClient.tsx`. The `[id]/page.tsx` route imports it from there.

**Legacy-only dossier UI (only imported by the file above)**

- `src/components/app/leads/dossier/DossierMarkdown.tsx` (~330 LOC)
- `src/components/app/leads/dossier/DossierSourceDrawer.tsx` (~629 LOC)
- `src/components/app/leads/dossier/use-dossier-sources.ts` (~87 LOC)
- `src/components/app/leads/dossier/source-registry.tsx` (~821 LOC)
- `src/components/app/leads/dossier/SourceChip.tsx` (~167 LOC)

**Not in the "8 deprecated components" list as discrete files**

Per `research/sdr-brain-v2-lead-detail-PLAN.md` §5.7 and the intro bullet (line ~25), the **eight deleted catalog names** are:

1. `IcpScoreRing`
2. `TriggerChip`
3. `OpeningHookBlock`
4. `CommercialInsightCard`
5. `ObjectionAccordion`
6. `PipelineStageStepper`
7. `InsightApplicationRow`
8. `PowerToolDrawer`

They are described as **inline** in `NbaCard` / legacy layout and removed in phase 7 — there is **no separate folder of eight `lead-detail-v2/*.tsx` files** to delete for V-M; they disappear with `LegacyLeadDetailClient` and refactors elsewhere.

**Explicitly out of scope for raw deletion (still used by v2)**

- `WebsiteIntelligencePanel`, `ReviewIntelligencePanel`, `VoiceNotesPanel`, `NbaCard`, `AiWorkersPanel`, `GoogleReviewsAccordion`, `LeadMapView`, `PlannerActions`, shared UI, APIs — v2 imports or lazy-loads these (`WhyNowBlock`, `HistoryBlock`, `WebsiteIntelLazyPanel`, etc.).
- `LegacyWorkersBeacon` — name suggests legacy but it still matters for `?tab=workers` on v2.

## Feature-flag plumbing for removal

- `src/app/app/leads/[id]/page.tsx` **~L24** — `import LegacyLeadDetailClient from "@/components/app/leads/LegacyLeadDetailClient";` (dead once legacy removed).
- `src/app/app/leads/[id]/page.tsx` **~L26–L28** — `LegacyWorkersBeacon` import stays; **`isLeadDetailV2Enabled`** import removable if always v2.
- `src/app/app/leads/[id]/page.tsx` **~L41–L46** — `Promise.all([..., cookies(), ...])` — **`cookies()`** only feeds the flag today; removable when flag goes.
- `src/app/app/leads/[id]/page.tsx` **~L48–L52** — `v2Enabled = isLeadDetailV2Enabled({ workspaceId }, { v: sp.v }, cookieStore)` — entire resolution block removable in v2-only world.
- `src/app/app/leads/[id]/page.tsx` **~L63–L69** — **`if (!v2Enabled) { return (<> … <LegacyLeadDetailClient id={id} /> …); }`** — the **fallback branch** called out in the master plan; this is the dead-code shape after the gate.
- `src/app/app/leads/[id]/page.tsx` **~L16–L18, L54–L57** — comments documenting `?v=1` / legacy workers tab — update with docs cleanup.
- `src/lib/feature-flags.ts` **~L1–L93** (lead-detail block) — **`LEAD_DETAIL_V2_COOKIE`**, **`FeatureFlagSearchParams`**, **`FeatureFlagCookieStore`**, **`pickFirst`**, **`fromUrl`**, **`fromCookie`**, lead-detail **`fromWorkspaceAllowList`**, **`fromEnvDefault`** (V2 default), **`isLeadDetailV2Enabled` — safe to remove** once nothing calls them, **provided** **`FeatureFlagSession`** and **`parseOnOff`** stay for Truth Layer (`isTruthLayerFlagEnabled` uses `parseOnOff`).
- **Do not confuse** with `src/lib/lead-detail/use-pipeline-stage.ts` **`LEAD_DETAIL_V2_EXPAND_RULES`** — naming overlap only; **not** env/cookie v2 surface flag; **keep** unless product changes block expand behavior.
- `src/__tests__/lead-detail/feature-flag.test.ts` — **entire file** becomes obsolete or must be deleted with the resolver.

**Follow-on (optional after v2-only, not strictly "legacy delete")**

- Hard-coded `?v=2` in `QueueStrip.tsx`, `AccountBlock.tsx`, e2e/mobile specs — can be dropped when URL override is meaningless.
- i18n `placeholderSubtitle` strings mentioning `?v=1` (`src/i18n/en/common.ts`, `src/i18n/tr/common.ts`).

## Hash redirect compatibility

- **Pure mapping:** `src/lib/lead-detail/legacy-hash-redirect.ts` — `getRedirectTarget(hash)` maps legacy hashes to `scroll` | `navigate` | `noop` (workers → `/workers` relative navigation).
- **Consumer:** `src/components/app/lead-detail-v2/LeadDetailV2Client.tsx` **~L257–L288** — `useEffect` reads `window.location.hash`, calls `getRedirectTarget`, applies via `applyHashAction`, emits telemetry.
- **v2-only world:** Redirect **continues to work** as long as **`LeadDetailV2Client` mounts** (i.e. the lead detail route always renders v2). No dependency on the legacy tab component for hash resolution.

## Telemetry gates to monitor before delete

There are **no** event names starting with `legacy_*` in `telemetry.ts`. Legacy-related signals use the **`lead_detail.legacy_*`** namespace:

- `lead_detail.legacy_hash_consumed` — props: `leadId`, `workspaceId`, `hash` (`src/lib/lead-detail/telemetry.ts` ~L101–L105, frozen list ~L280).
- `lead_detail.legacy_workers_link_followed` — props: `leadId`, `workspaceId?` (~L106–L109, ~L281).

**Pass criterion:** zero hits in the **most recent release**, then maintain a **14-day deprecation window** before removing legacy routes/aliases (per master plan / Open Decision §10.6).

## Recommended trigger conditions

1. PostHog (or equivalent) shows **zero** `lead_detail.legacy_hash_consumed` and **zero** `lead_detail.legacy_workers_link_followed` in production for the **shipping release** (or stricter: both zero for **14 consecutive days** after that release depending on how §10.6 is interpreted — coordinator should align with Release Manager).
2. Confirm **no** product/CS requirement for `?v=1` / cookie **`leadac_lead_detail_v2`** / env **`LEAD_DETAIL_V2_*`** rollback.
3. Remove or rewrite **tests** that assert legacy `TabsList` (`tests/e2e/lead-detail-flag.spec.ts` stub; any future un-skipped Playwright that compares `?v=1` vs `?v=2`).
4. After deletion, **manually or e2e-verify** hash table still passes `src/__tests__/lead-detail/legacy-hash-redirect.test.ts` scenarios against the **v2-only** page.

## Estimated diff size

- **N files deleted:** **6** (1 legacy client + **5** dossier modules) as the core UI bundle; **+1** test file (`feature-flag.test.ts`) likely deleted or heavily reduced.
- **M lines removed (order of magnitude):** **~4,784 LOC** in the six component files above, plus **~150–250 LOC** from `page.tsx` + `feature-flags.ts` + tests (rough).
